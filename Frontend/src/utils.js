// Utility for glitter effect (optional, for future use)
export function addGlitterEffect(element) {
  // This is a placeholder for a JS-based glitter effect if you want to add animated sparkles
  // For now, use CSS gradients and box-shadows for glittery look
}
